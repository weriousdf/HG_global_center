# 한결 글로벌문서 센터 — 인수인계

## 0. 목적

해외·국내 간 서류 처리(번역 → 공증 → 아포스티유/대사관 인증 → 발송)를 원스톱으로 제공하는
"한결 글로벌문서 센터"의 웹사이트. 최종 목표는 **사용자가 제출 국가와 서류 종류를
고르고 견적·상담을 신청하는 것**, 그리고 **그 신청서가 온라인 DB에 쌓이는 것**.

## 1. 지금 상태 (한 줄 요약)

목업 → **실동작 웹사이트**로 옮겼고, 견적 신청이 Supabase 에 실제로 저장되는 것까지
확인했다. 남은 큰 항목은 배포, 진행상황 조회 연결, 실제 금액·로고·사진.

여전히 **프로토타입 단계**다. 사용자가 "진짜데이터를 넣었어" 라고 말하기 전까지는
DB 안의 내용을 실제 고객 데이터로 취급하지 않는다 — 테이블을 다시 만들거나 테스트 행을
지우는 데 부담을 두지 않아도 된다.

프로젝트 현황·남은 일·아이디어는 [`README.md`](README.md), 셋업과 배포 절차는
[`SETUP.md`](SETUP.md) 에 있다. 이 문서는 **왜 그렇게 만들었는지**와 작업 이력이다.

## 2. 확정된 전제 (사용자 응답)

* 만들 화면: 랜딩(홈), 서비스 안내, 견적/상담 신청 폼, 서류 접수 진행상황 조회
* 주 타깃: **개인 B2C 우선** (기업 B2B/B2G는 병행 노출)
* 기준 디바이스: 데스크톱 + 모바일 둘 다
* 결과물 형태: **클릭 가능한 프로토타입** → 실동작 웹사이트로 승격
* 핵심 행동: 국가와 서류 종류 선택 → 견적 상담 신청
* 문구: 사용자가 준 원문은 그대로, 나머지는 작성 위임
* 변형(비교)이 필요한 축: **레이아웃 구조**, **신청 흐름 방식** (둘 다 URL 쿼리로 유지)
* 강조할 신뢰 요소: 처리 국가 수, 제휴 법무법인, 처리 기간·속도, 누적 처리 건수, 가격 투명성
* 제공 에셋: 없음 (로고·사진 미보유)
* **데이터 저장: Supabase** (사용자 계정 보유). 고객이 인터넷으로 접속해 남긴 신청서가
  사용자의 온라인 DB에 쌓이는 구조를 원한다.

## 3. 사용자가 준 원문 (수정 금지, 그대로 사용 중)

> **서비스명**: "한결 글로벌문서 센터" 다. 한때 "한결 글로벌문서 **솔루션** 센터" 로
> 적혀 있었으나 사용자 요청으로 전부 되돌렸다 (사이트 6곳 + 문서 3곳 + GitHub 저장소 설명).
> 아래 소개문의 "글로벌 문서 센터입니다" 도 마찬가지로 "솔루션" 이 들어가면 안 된다.

* 소개문: "한결 글로벌문서 센터는 해외 법인설립·주재원 파견·계약서·정관 등 기업
서류부터 부모동의서·유학·이민·외국인 비자까지, 국가 간 번역·공증·아포스티유·대사관 인증을
원스톱으로 처리하는 글로벌 문서 센터입니다."
* 기업 (B2B / B2G): "해외 법인설립, 주재원 파견, 해외 투자·진출에 필수적인 정관·계약서·
위임장(POA)·특허·경력 서류 등의 전문 번역부터 법무법인 공증 촉탁, 아포스티유 및 각국 대사관
인증까지 일괄 지원합니다."
* 개인 (B2C): "미성년자 부모여행동의서, 유학·이민 구비서류, 외국인 비자 발급 및 국내 체류 수속
서류의 번역·공증·인증 전 과정을 신속하고 정확하게 원스톱으로 해결합니다."
* 상담 안내 (하단 CTA 밴드): "서류 준비가 처음이라 잘 모르시더라도 괜찮습니다. / 플러스카톡,
오픈카톡, 전화 등 편하신 방법으로 연락해 주시면, / 현재 상황에 맞춰 준비하실 서류와 진행
과정을 알기 쉽게 안내해 드리겠습니다!"
  * 사용자가 세 줄로 끊어 준 것이라 넓은 화면에서는 그 줄바꿈을 유지한다
    (`<br class="wide-only">`). 좁은 화면에서는 그 br 을 숨겨 자연스럽게 흐르게 한다 —
    강제 줄바꿈을 남기면 세 줄이 각각 또 접혀 지저분해진다.
  * "플러스카톡" 은 사용자가 쓴 표현이다. 공식 명칭("카카오톡 채널")으로 바꾸지 않는다.
  * 이 문구는 **두 곳**에 있다 — 랜딩 최상단 인포그래픽 배너(`introBanner()`)와 하단 CTA
    밴드. 사용자가 "중요한 글" 이라 하여 도착 직후와 다 읽은 뒤 양쪽에 뒀다.
  * 한때 카카오 버튼과 같은 조건(`kakaoLinks().length ? …`)에 묶여 있어서 config 의 카카오
    주소가 비면 문구까지 사라졌다. 문구는 전화도 안내하므로 조건 없이 항상 표시한다.

## 4. 확정된 수치 (사용자 지정)

* 처리 국가: **아포스티유 협약국 전체** + 대사관 인증 **20개국**
* 처리량: **연 3,000건** (기업·개인 합계)
* 가격 정책 문구: 신청 시 절차와 총액을 사전에 고정
* 미확정(플레이스홀더 상태): 제휴 법무법인 수, 비용 표의 실제 금액

## 5. 비주얼 기준

* Organic 디자인 시스템(크림/샌드 배경 #f5ead8, 테라코타 #c67139, 세이지 #7a8a5e, 큰 라운드,
필 버튼)을 그대로 사용. 색·간격·컴포넌트는 시스템 토큰과 클래스(.btn/.card/.tag/.table/.input)에서만 가져옴.
* 한글 폰트 보강: 제목 Jua, 본문 Noto Sans KR (원 시스템의 Caprasimo/Figtree는 한글 글리프가
없어 라틴 전용으로 남김)
* 사진 없음 → 여백과 원형 도형으로 처리. 이미지 생성은 불가하므로 실제 사진 확보 필요.

## 6. 구조 결정 — 왜 목업을 옮겼는가

원래 목업은 `한결 글로벌문서 프로토타입.dc.html` (디자인 캔버스 형식) 하나였다. 여기에는
데이터를 저장할 방법이 없다. 또한 이 파일을 Claude Artifact 로 발행해도 **Artifact 페이지는
보안 정책(CSP)상 외부 서버로의 fetch/XHR 이 전부 차단**되어 Supabase 를 호출할 수 없다.

그래서 `docs/` 에 빌드 도구 없는 일반 정적 웹사이트를 새로 만들고, 목업의 화면·문구·로직을
그대로 옮겼다. 목업 파일은 참고용으로 남겨 두었다.

디자인 비교용 변형(레이아웃 A/B, 위저드/한화면)은 버리지 않고 **URL 쿼리**로 옮겼다.
고객에게는 검토 UI 가 보이지 않고, `?preview=1` 을 붙였을 때만 전환 바가 뜬다.
기본값은 레이아웃 A + 단계형 위저드.

## 7. 데이터 저장 설계

`supabase/schema.sql` 을 Supabase SQL Editor 에 한 번 실행하면 다음이 만들어진다.

* **`applications` 테이블** — `order_no`(접수번호, 유일), `name`, `phone`, `email`,
  `country`, `doc_type`, `cert_type`, `memo`, `status`(기본값 `접수`), `created_at`
  * `country` 는 칩에 없는 나라를 고객이 직접 적을 수 있어(아래 참고) **자유 입력**이
    섞인다. 칩으로 고른 경우와 직접 적은 경우가 같은 칸에 들어간다 — `'기타'` 라는
    값이 저장되는 일은 없고, 항상 실제 나라 이름이 들어간다.
* **`application_order_seq` 시퀀스** — 접수번호 `HG-YYMM-0001` 의 뒷번호. 한국 시간 기준.
* **RLS 로 테이블 잠금 + 정책 0개** — 익명 방문자(anon 키)는 테이블을 직접 읽거나 쓸 수 없다.
* **`submit_application()` 함수** (`security definer`) — 필수값·길이를 검증하고 insert 한 뒤
  **접수번호만** 돌려준다.

이 설계의 요점: 웹페이지에 들어가는 `anon` 키로 할 수 있는 일이 "저장" 하나뿐이라는 것.
`insert ... returning` 을 쓰면 SELECT 권한이 필요해지고 그러면 남의 신청서도 읽히므로,
함수로 감싸서 접수번호만 흘려보낸다. 관리자 조회는 Supabase 대시보드에서 한다.

`service_role` 키는 웹페이지에 절대 넣지 않는다 — 넣으면 누구나 전체 신청서를 볼 수 있다.

## 7-1. 제출 국가 · 서류 종류의 '기타' 직접 입력

사용자 지적으로 넣었다: "아포스티유 협약국 전체 + 대사관 인증 20개국" 을 처리한다고
해 놓고 칩에는 8개국만 있어서, 목록에 없는 나라 고객이 신청할 방법이 없었다. 서류 종류도
같은 문제라 함께 넣었다.

* 두 칩 목록 끝에 **기타** 가 있고, 누르면 직접 입력칸이 나타나며 커서까지 들어간다.
* 칩이 나오는 **모든 곳**에서 동작한다 — 랜딩 히어로(국가), 위저드 1·2단계, 한화면형.
* 저장되는 값은 `'기타'` 가 아니라 **직접 적은 이름**이다 (`picked('country')`,
  `picked('doc')`). DB 에 `'기타'` 라는 값이 들어가는 일은 없다.
* `기타` 를 골랐는데 이름이 비면 다음 단계로 못 넘어가고, 버튼과 오류 문구가
  "선택해 주세요" 가 아니라 "…이름을 입력해 주세요" 로 바뀐다 (이미 골랐으니까).
* 길이 제한은 국가 40자 · 서류 60자. `maxlength` 와 JS 검사와 `schema.sql` 의 서버 검사
  세 겹이다. **서버 검사를 반영하려면 함수를 다시 실행해야 한다** — `schema.sql` 은
  `create or replace` 라 전체를 다시 Run 해도 안전하다.

구현 메모:

* 두 항목이 같은 구조라 `OTHER_FIELDS` 표 하나에 항목별 설정(입력 상태 이름, 라벨,
  placeholder, 최대 길이)을 모아 두고 `otherField(key)` · `picked(key)` ·
  `otherProblem(key)` 가 그 표를 읽는다. 세 번째 항목이 생기면 표에 한 줄 더 넣으면 된다.
* 입력 중에는 화면을 다시 그리지 않는다(커서와 한글 조합이 끊긴다). 대신 `syncLive()` 가
  `[data-live="country"|"doc"]` 표시와 진행 버튼만 직접 손본다.
* 버튼 문구와 차단 사유는 `stepButtonLabel()` · `stepProblem()` 한 곳에서 계산한다.
  그릴 때와 갱신할 때가 어긋나면 "다 채웠는데 버튼이 안 바뀌는" 증상이 나온다.

## 7-2. 관리자 화면과 권한 설계

사용자 요청으로 붙였다: "관리자도 웹페이지에서 관리자키를 입력하고 들어가면 신청된
내용이 보이면 좋겠다." 그전까지는 Supabase 대시보드가 유일한 조회 경로였다.

### 인증 방식 — 왜 '관리자키 하나' 가 아닌가

사용자에게 세 안을 제시하고 **이메일 + 비밀번호 로그인**(Supabase Auth)을 골랐다.

먼저 배제한 것: **브라우저 JavaScript 에서 키를 비교하는 방식은 무의미하다.** 페이지
소스를 열면 키가 그대로 보인다. 검증은 반드시 서버에서 해야 한다.

### 권한은 "로그인한 사람" 이 아니라 "명단에 있는 사람" 기준

Supabase 는 회원가입이 열려 있으면 누구나 계정을 만들 수 있다. 그래서
`for select to authenticated using (true)` 로 정책을 쓰면 **아무나 가입해서 신청서를
전부 읽을 수 있다.** 대시보드의 회원가입 토글에 의존하는 것도 불안하다.

그래서 `public.admins` 표를 두고, 그 표에 있는 계정만 읽게 했다.

* `admins` 는 RLS 를 켜고 정책을 만들지 않는다 + `anon`·`authenticated` 권한 회수
  → API 로는 아무도 손댈 수 없고, 대시보드(SQL Editor)에서만 관리한다.
* 정책 안에서 `admins` 를 직접 조회하면 **`admins` 자신의 RLS 에 걸려 항상 거짓**이
  된다. 그래서 `is_admin()` 을 `security definer` 로 만들어 감쌌다. 이 부분이 헷갈리기
  쉬우니 정책을 손볼 때 주의.
* `applications` 의 SELECT 권한은 `authenticated` 에게만 준다. `anon` 은 여전히 0권한 —
  고객은 저장 함수 호출만 가능하다.

증상 대응: **로그인은 되는데 목록이 비어 있으면** 그 계정이 `admins` 에 없는 것이다.
오류가 아니라 0건으로 오기 때문에 헷갈린다. 화면에도 이 안내를 넣어 두었다.

### 화면 (`docs/admin.html`, `admin.js`, `admin.css`)

고객 화면과 파일을 나눴다 — 관리자 코드가 고객 브라우저로 내려가지 않는다.

* 로그인 → 접수 목록(최신순) 표, 검색(이름·연락처·접수번호·국가·서류·요청사항),
  새로 고침, CSV 내려받기(엑셀 한글 깨짐 방지용 BOM 포함)
* 토큰은 `localStorage` 에 두어 새로 고쳐도 로그인이 유지된다. 만료가 가까우면
  refresh token 으로 갱신하고, 갱신도 실패하면 로그아웃 안내를 띄운다.
* 검색은 다시 그리지 않고 행을 숨기고 건수만 갱신한다 (고객 화면과 같은 이유 — 커서와
  한글 조합이 끊긴다).
* `noindex` + `robots.txt` 로 검색엔진 차단. 주소를 아는 사람은 로그인 화면까지 볼 수
  있고, 그 뒤는 Supabase 가 막는다.

**주의**: 이 기능도 `schema.sql` 재실행이 필요하다 (5~7번 항목).

### 동작 확인 완료

사용자가 관리자 계정을 만들고 `admins` 에 등록한 뒤 로그인해 접수 목록이 보이는 것까지
확인했다. 즉 아래 세 갈래 권한이 전부 의도대로 동작한다.

| 누구 | 무엇으로 | 결과 |
| --- | --- | --- |
| 고객 | 공개 키 (`sb_publishable_...`) | 저장 함수 호출만 가능. 읽기·직접 쓰기 401 |
| 가입만 한 계정 | 로그인 토큰 | 조회 결과 0건 (`admins` 에 없음) |
| 관리자 | 로그인 토큰 + `admins` 등록 | 전체 조회 |

## 7-3. 진행상황 조회 — 휴대폰 번호로만 조회한다

사용자가 목업의 구멍을 먼저 지적했다: "고객이 자신의 접수번호를 입력하면 바로 조회가
되게되어있잖아? 그런데 사실 반쯤 뚫려있는 느낌이야. (…) 다른사람들이 뻔한 루틴의
접수번호로 타인의 진행상황을 알수있는건 문제야."

맞는 지적이다. 접수번호는 `HG-2609-0001`, `0002` … 로 순차 발급되므로 남의 번호를 찍기
쉽다. (다만 그 시점에 실제 유출은 없었다 — 목업이라 DB 를 조회하지 않았다.)

### 왜 '접수번호 + 휴대폰번호' 가 아닌가

내가 처음 만들려 한 것은 둘 다 받아 대조하는 방식이었는데, 사용자가 중단시켰다:
"자신의 휴대폰번호만 입력하면 되는걸로 해줘. 접수번호+휴대폰번호 까지 입력하는건 번거로워."

**휴대폰 번호 하나만 받는 것이 실제로 더 안전하다.** 탐색 공간을 비교하면

| 조회 열쇠 | 경우의 수 |
| --- | --- |
| 접수번호만 | 수백 가지 (순차 발급) |
| 휴대폰 번호만 | 1억 가지 (`010-XXXX-XXXX`) |

고객이 채울 칸은 똑같이 하나다. 입력을 늘리지 않고 문제가 해결됐다. "두 개를 받으면 두 배
안전하다" 는 반사적 결론을 검토 없이 채택하지 않는 것이 요점이다.

### 설계

* `track_applications(p_phone)` — `security definer`, 정규화한 번호가 일치하는 신청을
  최신순으로 돌려준다. 그 번호로 접수된 신청이 여러 건이면 전부 나온다.
* `normalize_phone()` 로 `010-1234-5678` · `01012345678` · `+82 10-1234-5678` 을 같게 본다.
  조회가 잦으므로 정규화된 번호에 인덱스를 걸었다.
* 자리수가 9자 미만이면 조용히 0건 — 짧은 번호로 훑는 시도를 돕지 않는다.
* **돌려주는 데이터를 최소로 줄였다** — 이름은 첫 글자만 남기고 마스킹(`홍**`), 연락처·
  이메일·요청사항은 아예 내보내지 않는다. 진행상황을 보는 데 필요 없는 개인정보다.
* `status` 에 CHECK 제약을 걸었다 (`접수/번역/공증/인증/발송/완료/취소`). 관리자가 상태를
  바꿀 때 오타가 들어가면 고객 화면의 단계 표시가 깨지기 때문이다.

### 화면

* 입력칸은 휴대폰 번호 하나. 화면에 "접수번호는 필요하지 않습니다" 와 그 이유를 적어 두었다
  — 고객이 접수번호를 찾다가 헤매지 않게.
* 5단계 타임라인을 `status` 에서 계산한다 (완료/진행중/대기). 인증 단계 이름은 그 신청의
  인증 방식(아포스티유 / 대사관 인증)을 그대로 보여 준다.
* 신청 완료 화면의 "진행상황 조회" 버튼은 방금 남긴 번호를 채워 바로 조회한다.
* 찾지 못했을 때는 원인을 구분해 알려주지 않는다 (번호가 없는 것과 틀린 것을 같게 취급).

### 오류 문구를 고객에게 그대로 보여주지 않는다

사용자 지적: 조회 실패 시 "Could not find the function public.track_applications(p_phone)
in the schema cache" 가 화면에 그대로 떴다. 고객이 할 수 있는 일이 없고 내부 구조만
드러난다.

`friendlyError(raw, fallback)` 로 갈랐다. 판별은 **한글이 섞였는지**로 한다 — 우리가
일부러 던지는 검증 메시지("이름을 입력해 주세요." 등)는 모두 한글이라 고객이 읽고 고칠
수 있으니 그대로 보여 주고, 영문 기술 메시지는 일반 문구로 바꾼다. 원인은
`console.error` 로 남겨 개발자가 확인할 수 있게 한다.

관리자 화면(`admin.js`)은 반대로 원문을 보여 준다 — 운영자는 원인을 알아야 한다.

### 연락처 저장 형식 통일

사용자 요청: "고객들이 010-1234-9876 으로 입력하든, 01012349876 으로 입력하든 같은
값으로 입력받은걸로해줘."

조회는 원래부터 서버에서 숫자만 뽑아 비교하므로 동작에는 문제가 없었다. 다만 저장되는
문자열이 제각각이라 관리자 목록에서 번호가 지저분하게 보였다. `formatPhone()` 으로
저장 시점에 맞춘다.

| 입력 | 저장 |
| --- | --- |
| `01012349876` · `010 1234 9876` | `010-1234-9876` |
| `0212345678` | `02-1234-5678` |
| `+82 10-1234-9876` · 그 밖의 형태 | 적은 대로 |

국제번호를 어설프게 끊으면 오히려 못 알아보게 되므로 국내 휴대폰(01…)과 서울 국번(02)
만 하이픈을 넣는다.

**주의**: `schema.sql` 재실행이 필요하다 (8·9번 항목).

## 7-4. 새 신청 알림 메일

사용자 요청. 관리자 화면을 계속 열어 두지 않아도 신청이 들어온 것을 알 수 있게 한다.

### 왜 대시보드의 Database Webhooks 를 안 썼나

대시보드 Webhooks 도 내부적으로 같은 `pg_net` 트리거다. 다만 그쪽은 Supabase 가 정한
형식(`{type, table, record, old_record}`)의 payload 만 보내므로, 이메일 서비스가 원하는
형태로 바꿔 줄 Edge Function 을 따로 배포해야 한다. Edge Function 배포는 Supabase CLI
설치가 필요해서, SQL 실행에 이미 익숙해진 사용자에게는 오히려 단계가 늘어난다.
SQL 트리거로 쓰면 필요한 형태를 바로 만들어 보내므로 중간 단계가 없다.

### 설계

* `app_settings` 표에 `resend_api_key` · `notify_email`(· 선택 `notify_from`)을 둔다.
  `admins` 와 같은 방식으로 잠가서 API 로는 아무도 읽을 수 없다.
* `notify_new_application()` 트리거가 `net.http_post` 로 Resend API 를 호출한다.
* **`pg_net` 은 요청을 큐에 넣고 바로 돌아온다.** 그래서 메일이 느리거나 실패해도 신청서
  저장은 영향받지 않는다. 고객 쪽이 막히면 안 되므로 이 점이 중요하다.
  거기에 `exception when others` 로 한 번 더 감쌌다.
* **설정이 없으면 조용히 넘어간다.** 알림을 붙이지 않았다고 신청 저장이 실패하면 안 된다.
* `html_escape()` 로 이름·요청사항을 이스케이프한다. 요청사항은 2000자 자유 입력이라
  `<` 가 들어오면 메일 서식이 깨진다.
  이 함수는 **NULL 을 NULL 로 남긴다** — 본문에서 "값이 없으면 표의 행 자체를 뺀다" 는
  처리가 concat 결과가 NULL 이 되는 것에 기대고 있어서, 여기서 `''` 로 바꾸면 빈 행이
  생긴다. (처음에 `coalesce(t,'')` 로 썼다가 이 이유로 고쳤다.)
* `to_char` 서식의 한글은 큰따옴표로 감쌌다 (`'MM"월" DD"일" HH24:MI'`).

### Resend 를 고른 이유

도메인 인증 없이도 **가입한 본인 이메일로는** 바로 보낼 수 있다. 관리자 알림은 본인에게
가는 것이므로 도메인 준비 없이 끝난다. 무료 한도는 하루 100통 (연 3,000건 예상이면
하루 평균 10건 안쪽이라 충분하다).

발송 결과는 `net._http_response` 표에서 본다. `422` 가 나오면 받는 주소가 Resend 가입
이메일이 아닌 것이다.

### 동작 확인 완료

사용자가 Resend 가입과 키 등록을 마친 뒤 실제로 메일을 받았다. SQL 도 실제 데이터베이스에
정상 실행됐다.

가는 길에 한 번 막혔던 지점: 12번 항목을 실행할 때 `re_여기에_키` 자리표시를 그대로 두고
넣어서 Resend 가 `400 API key is invalid` 를 돌려줬다. 이때 **신청서 저장은 정상이었다** —
메일 발송이 저장을 막지 않게 만든 부분이 실제로 작동한 것을 확인했다.

같은 곳에서 또 걸리지 않게 트리거에 `btrim` 과 `re_` 접두어 검사를 넣었다. 자리표시가
남아 있으면 Resend 를 두드리지 않고 조용히 넘어간다.

Resend 는 목록 화면에서 키를 가려서 보여 준다(`re_abc...`). 전체 키는 만드는 순간에만
보이므로, 다시 필요하면 새로 만들어야 한다.

> 알림 메일에는 고객 이름·연락처가 담긴다. 담당자가 바로 연락하려면 필요하지만, 그만큼
> 그 메일함과 Resend 계정도 고객 정보를 담는 곳이 된다.

## 7-5. 최상단 안심 안내 배너 (인포그래픽)

사용자 요청: "이 문구를 인포그래픽으로 만들어서 웹페이지 최상단에 붙여서 사람들이 편하게
받아들일수있도록 하고싶어. 가로형 직사각형모양으로."

### 이미지 파일로 만들지 않은 이유

가로형 직사각형이라 SVG 나 PNG 로 만들 수도 있었지만 HTML/CSS 로 그렸다.

* 이미지는 폭이 줄면 글씨도 같이 작아진다. 375px 화면에서 가로 배너를 축소하면 읽을 수
  없어진다. HTML 은 좁아지면 3단계를 세로로 쌓아 글씨 크기를 지킨다.
* 문구를 고칠 때 그림을 다시 만들지 않아도 된다. 이 문구는 이미 두 번 바뀌었다.
* 검색엔진이 텍스트로 읽는다. 이미지 안의 글자는 못 읽는다.

### 구성

왼쪽은 사용자가 준 원문(첫 문장을 제목 크기로, 나머지 두 줄을 굵은 본문으로 — 글자는
바꾸지 않고 크기만 나눴다), 오른쪽은 연락 → 상황 확인 → 안내 3단계 흐름이다.
아이콘과 화살표는 인라인 SVG 로 직접 그렸다 (외부 아이콘 묶음 의존 없음).

3단계 제목은 짧게(`연락`·`상황 확인`·`안내`) 두고 내용은 설명줄에 담았다. 오른쪽 칸을
셋으로 나누면 한 칸이 150px 남짓이라, 제목이 길면 글자가 꽉 차서 답답해진다.

`word-break: keep-all` 로 한글이 단어 중간에서 끊기지 않게 했다.

### 반응형

| 폭 | 배치 |
| --- | --- |
| 900px 초과 | 왼쪽 문구 / 오른쪽 3단계 가로 배치 |
| 720–900px | 문구 위, 3단계 아래 (가로 유지) |
| 720px 이하 | 3단계를 세로로 쌓고 화살표를 숨긴다. 아이콘은 왼쪽, 글자는 오른쪽 |

### 만들면서 걸린 것

`.intro-step-body` 를 모바일 미디어쿼리 안에서만 `flex-direction: column` 으로 뒀더니,
데스크톱에서는 `span` 기본값인 inline 이 적용되어 제목과 설명이 한 줄에 붙어 나왔다.
기본 상태에 선언하고 모바일에서는 간격만 조정하도록 고쳤다.

## 8. 검증한 것

로컬 서버(`python -m http.server`)에 띄우고 브라우저로 확인했다.

* 4개 화면 이동, 레이아웃 A/B, 위저드/한화면 흐름, 모바일(375px) 레이아웃 — 정상
* 위저드 4단계(국가 → 서류 → 인증 → 연락처) 진행과 단계별 진행 차단 — 정상
* 이름·연락처를 채우면 제출 버튼 문구·색이 즉시 바뀜 (타이핑 중에는 다시 그리지 않고
  버튼만 직접 갱신한다 — 다시 그리면 커서와 한글 조합이 끊긴다)
* `config.js` 가 비어 있으면 안내 배너가 뜨고 제출이 막힘 — 정상
* fetch 를 가짜로 세워 제출 경로 확인: RPC 주소·헤더·payload(이름·연락처 포함) 정확,
  서버가 준 접수번호가 완료 화면에 표시됨
* 오류 경로 2종(연락처 자리수 부족 → 클라이언트 검사 / 서버 400 → 서버 메시지 노출) — 정상

### 실제 Supabase 연결 확인 (완료)

사용자가 Supabase 프로젝트를 만들고 `schema.sql` 을 실행한 뒤, 로컬에서 신청을 한 건
넣어 확인했다.

* 접수번호 **HG-2609-0001** 발급 — 시퀀스와 한국 시간 기준 `YYMM` 형식 정상
* 공개 키로 `applications` 테이블 **직접 읽기** 시도 → 401 `permission denied`
* 공개 키로 테이블 **직접 쓰기** 시도 → 401 `permission denied`
* `submit_application()` 호출만 성공 — 의도한 대로 저장 외에는 아무것도 못 한다

확인용 테스트 행이 몇 건 들어가 있다. 이름에 `테스트-삭제해도됨` 이 붙어 있으니 지워도 된다.

```sql
delete from applications where name like '%테스트-삭제해도됨%';
```

**설정 값에서 걸린 것 두 가지 (다음에 헷갈리지 않도록)**

1. `config.js` 의 키에 따옴표를 빼면 JavaScript 가 문자열이 아니라 변수 이름으로 읽어서
   `window.HG_CONFIG` 자체가 만들어지지 않는다. 화면에는 "연결 설정 대기 중" 만 뜬다.
2. `Project Settings → API` 페이지에는 **키만 있고 Project URL 이 없다.** 사용자가
   여기서 URL 을 찾다가 막혔다 (두 번째로 같은 곳에서 막혔다고 했다). URL 은 메뉴에서
   찾지 말고 주소창의 프로젝트 ref 로 만드는 게 확실하다 —
   `.../dashboard/project/<ref>` → `https://<ref>.supabase.co`.
   라벨로 표시된 건 `Project Settings → Data API` 에 있지만 Supabase 가 이 메뉴 이름을
   자주 바꾼다. 키는 *Publishable key* (`sb_publishable_...`, 예전 anon/public) 를 쓰고,
   같은 화면의 *Secret key* (`sb_secret_...`, 예전 service_role) 는 절대 쓰지 않는다.

## 9. 남은 일

1. **배포** — GitHub Pages. 저장소는 만들어 두었다
   (github.com/weriousdf/HG_global_center, Public). Settings → Pages 에서
   Source 를 `main` / `/docs` 로 지정하면 된다.

   사이트 폴더가 `web/` 이 아니라 `docs/` 인 이유: GitHub Pages 의 "Deploy from a
   branch" 방식은 폴더를 저장소 루트 `/` 와 `/docs` **두 곳만** 지원한다. 임의 폴더명은
   드롭다운에 아예 나오지 않는다. `web/` 이름을 지키려면 GitHub Actions 워크플로로
   배포해야 하는데, 프로토타입 단계에서 CI 를 하나 더 두는 것보다 폴더명을 맞추는 쪽이
   단순해서 이름을 바꿨다.
2. ~~진행상황 조회 연결~~ — 완료. 7-3 항목 참고.
3. 비용 표 실제 금액, 제휴 법무법인 수 입력 (현재 `[00,000]원` 자리표시)
4. 로고·사진 확보 후 히어로/서비스 안내에 반영
5. 서비스 안내를 서류별 상세(필요 서류, 준비물, 국가별 유의사항)로 확장
6. 신청 폼 확장: 부수, 희망 수령일, 수령 방법(방문/등기/해외 발송), 파일 업로드
   (파일은 Supabase Storage 를 쓰면 된다)
7. 사업자 정보·연락처·개인정보 처리방침 등 푸터 확정
8. ~~신규 신청 알림~~ — 완료. 실제 메일 수신까지 확인. 7-4 항목 참고.
9. 관리자 화면에서 **상태 변경** — 지금은 읽기만 된다. `status` 가 계속 `접수` 로 남아
   있어서 고객이 진행상황을 조회해도 1단계만 보인다. 상태를 넘기려면 대시보드 Table
   Editor 에서 직접 바꿔야 한다. UPDATE 정책과 화면을 붙이는 것이 다음 순서로 가장
   자연스럽다.
9. **실데이터 전환 시 정리할 것** — 사용자가 "진짜데이터를 넣었어" 라고 말하기 전까지는
   전부 프로토타입 단계로 취급한다. 전환 시점에 함께 정리할 목록:
   * 테스트 행 `HG-2609-0001` 삭제, 시퀀스 초기화 여부 결정
   
   * 비용 표의 `[00,000]원` 자리표시
   * `docs/index.html` 의 `noindex` 메타와 `docs/robots.txt` — 지금은 검색엔진 수집을
     전면 차단해 두었다. 문구가 확정되기 전에 검색 결과에 뜨면 안 되기 때문이다.

## 10. 파일

| 경로 | 무엇인가 |
| --- | --- |
| `docs/index.html` | 고객 화면 껍데기 · 상단 내비 · 푸터 |
| `docs/app.js` | 고객 화면 4개 · 상태 · 검증 · Supabase 저장 |
| `docs/admin.html` · `admin.js` · `admin.css` | 관리자 화면 — 로그인, 접수 목록, 검색, CSV |
| `docs/robots.txt` | 프로토타입 단계 수집 차단 |
| `docs/site.css` | 사이트 전용 스타일 (디자인 시스템 위에 얹음) |
| `docs/organic.css` | Organic 디자인 시스템 (`_ds/` 에서 복사) |
| `docs/config.js` | Supabase URL · Publishable 키 — 입력 완료, 연결 확인됨 |
| `supabase/schema.sql` | 테이블 · 시퀀스 · RLS · 저장/조회 함수 · 관리자 권한 · 알림 트리거 |
| `README.md` | 프로젝트 현황 — 완성된 것 · 보완할 것 · 아이디어 |
| `SETUP.md` | 셋업 · 배포 · 신청서 조회 · 알림 설정 |
| `.claude/launch.json` | 로컬 미리보기 서버 설정 (포트 5173) |
| `한결 글로벌문서 프로토타입.dc.html` | 초기 디자인 목업 (참고용, 저장 기능 없음) |
| `_ds/organic-.../` | 디자인 시스템 원본 |
| `ios-frame.jsx`, `image-slot.js` | 목업에서 쓰던 부품 |
