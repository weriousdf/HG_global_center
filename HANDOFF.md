# 한결 글로벌문서 센터 — 인수인계

## 0. 목적

해외·국내 간 서류 처리(번역 → 공증 → 아포스티유/대사관 인증 → 발송)를 원스톱으로 제공하는
"한결 글로벌문서 솔루션 센터"의 웹사이트. 최종 목표는 **사용자가 제출 국가와 서류 종류를
고르고 견적·상담을 신청하는 것**, 그리고 **그 신청서가 온라인 DB에 쌓이는 것**.

## 1. 지금 상태 (한 줄 요약)

목업 → **실동작 웹사이트**로 옮겼고, 견적 신청이 Supabase 에 저장된다.
남은 큰 항목은 진행상황 조회 연결, 실제 금액·로고·사진, 배포.

셋업 절차와 배포 방법은 [`README.md`](README.md) 에 있다. 이 문서는 결정 사항과 이력이다.

## 2. 확정된 전제 (사용자 응답)

* 만들 화면: 랜딩(홈), 서비스 안내, 견적/상담 신청 폼, 서류 접수 진행상황 조회
* 주 타깃: **개인 B2C 우선** (기업 B2B/B2G는 병행 노출)
* 기준 디바이스: 데스크톱 + 모바일 둘 다
* 결과물 형태: **클릭 가능한 프로토타입** → 실동작 웹사이트로 승격
* 핵심 행동: 국가와 서류 종류 선택 → 견적 상담 신청
* 문구: 사용자가 준 원문은 그대로, 나머지는 작성 위임
* 변형(비교)이 필요한 축: **레이아웃 구조**, **신청 흐름 방식** (둘 다 URL 쿼리로 유지)
* 강조할 신뢰 요소: 처리 국가 수, 제휴 법무법인, 처리 기간·속도, 누적 처리 건수, 가격 투명성
* 제공 에셋: 없음 (로고·사진 미보유)
* **데이터 저장: Supabase** (사용자 계정 보유). 고객이 인터넷으로 접속해 남긴 신청서가
  사용자의 온라인 DB에 쌓이는 구조를 원한다.

## 3. 사용자가 준 원문 (수정 금지, 그대로 사용 중)

* 소개문: "한결 글로벌문서 센터는 해외 법인설립·주재원 파견·계약서·정관 등 기업
서류부터 부모동의서·유학·이민·외국인 비자까지, 국가 간 번역·공증·아포스티유·대사관 인증을
원스톱으로 처리하는 글로벌 문서 센터입니다."
* 기업 (B2B / B2G): "해외 법인설립, 주재원 파견, 해외 투자·진출에 필수적인 정관·계약서·
위임장(POA)·특허·경력 서류 등의 전문 번역부터 법무법인 공증 촉탁, 아포스티유 및 각국 대사관
인증까지 일괄 지원합니다."
* 개인 (B2C): "미성년자 부모여행동의서, 유학·이민 구비서류, 외국인 비자 발급 및 국내 체류 수속
서류의 번역·공증·인증 전 과정을 신속하고 정확하게 원스톱으로 해결합니다."

## 4. 확정된 수치 (사용자 지정)

* 처리 국가: **아포스티유 협약국 전체** + 대사관 인증 **20개국**
* 처리량: **연 3,000건** (기업·개인 합계)
* 가격 정책 문구: 신청 시 절차와 총액을 사전에 고정
* 미확정(플레이스홀더 상태): 제휴 법무법인 수, 비용 표의 실제 금액

## 5. 비주얼 기준

* Organic 디자인 시스템(크림/샌드 배경 #f5ead8, 테라코타 #c67139, 세이지 #7a8a5e, 큰 라운드,
필 버튼)을 그대로 사용. 색·간격·컴포넌트는 시스템 토큰과 클래스(.btn/.card/.tag/.table/.input)에서만 가져옴.
* 한글 폰트 보강: 제목 Jua, 본문 Noto Sans KR (원 시스템의 Caprasimo/Figtree는 한글 글리프가
없어 라틴 전용으로 남김)
* 사진 없음 → 여백과 원형 도형으로 처리. 이미지 생성은 불가하므로 실제 사진 확보 필요.

## 6. 구조 결정 — 왜 목업을 옮겼는가

원래 목업은 `한결 글로벌문서 프로토타입.dc.html` (디자인 캔버스 형식) 하나였다. 여기에는
데이터를 저장할 방법이 없다. 또한 이 파일을 Claude Artifact 로 발행해도 **Artifact 페이지는
보안 정책(CSP)상 외부 서버로의 fetch/XHR 이 전부 차단**되어 Supabase 를 호출할 수 없다.

그래서 `web/` 에 빌드 도구 없는 일반 정적 웹사이트를 새로 만들고, 목업의 화면·문구·로직을
그대로 옮겼다. 목업 파일은 참고용으로 남겨 두었다.

디자인 비교용 변형(레이아웃 A/B, 위저드/한화면)은 버리지 않고 **URL 쿼리**로 옮겼다.
고객에게는 검토 UI 가 보이지 않고, `?preview=1` 을 붙였을 때만 전환 바가 뜬다.
기본값은 레이아웃 A + 단계형 위저드.

## 7. 데이터 저장 설계

`supabase/schema.sql` 을 Supabase SQL Editor 에 한 번 실행하면 다음이 만들어진다.

* **`applications` 테이블** — `order_no`(접수번호, 유일), `name`, `phone`, `email`,
  `country`, `doc_type`, `cert_type`, `memo`, `status`(기본값 `접수`), `created_at`
* **`application_order_seq` 시퀀스** — 접수번호 `HG-YYMM-0001` 의 뒷번호. 한국 시간 기준.
* **RLS 로 테이블 잠금 + 정책 0개** — 익명 방문자(anon 키)는 테이블을 직접 읽거나 쓸 수 없다.
* **`submit_application()` 함수** (`security definer`) — 필수값·길이를 검증하고 insert 한 뒤
  **접수번호만** 돌려준다.

이 설계의 요점: 웹페이지에 들어가는 `anon` 키로 할 수 있는 일이 "저장" 하나뿐이라는 것.
`insert ... returning` 을 쓰면 SELECT 권한이 필요해지고 그러면 남의 신청서도 읽히므로,
함수로 감싸서 접수번호만 흘려보낸다. 관리자 조회는 Supabase 대시보드에서 한다.

`service_role` 키는 웹페이지에 절대 넣지 않는다 — 넣으면 누구나 전체 신청서를 볼 수 있다.

## 8. 검증한 것

로컬 서버(`python -m http.server`)에 띄우고 브라우저로 확인했다.

* 4개 화면 이동, 레이아웃 A/B, 위저드/한화면 흐름, 모바일(375px) 레이아웃 — 정상
* 위저드 4단계(국가 → 서류 → 인증 → 연락처) 진행과 단계별 진행 차단 — 정상
* 이름·연락처를 채우면 제출 버튼 문구·색이 즉시 바뀜 (타이핑 중에는 다시 그리지 않고
  버튼만 직접 갱신한다 — 다시 그리면 커서와 한글 조합이 끊긴다)
* `config.js` 가 비어 있으면 안내 배너가 뜨고 제출이 막힘 — 정상
* fetch 를 가짜로 세워 제출 경로 확인: RPC 주소·헤더·payload(이름·연락처 포함) 정확,
  서버가 준 접수번호가 완료 화면에 표시됨
* 오류 경로 2종(연락처 자리수 부족 → 클라이언트 검사 / 서버 400 → 서버 메시지 노출) — 정상

**아직 실제 Supabase 로는 넣어 보지 못했다.** `config.js` 에 키가 없기 때문이다.
README 1~3단계를 따라 키를 넣고 신청을 한 번 넣어 보는 것이 다음 확인 항목이다.

## 9. 남은 일

1. **README 1~3단계 실행** — SQL 실행, `config.js` 채우기, 실제 저장 확인 (사용자 작업)
2. **배포** — GitHub Pages. 저장소를 만들어야 하므로 `gh auth login` 은 사용자가 직접.
3. **진행상황 조회 연결** — 접수번호 + 휴대폰 뒷자리로 본인 확인하는 `track_application()`
   함수를 추가하고 화면을 붙인다. 현재는 예시 데이터 + "준비 중" 안내문 상태.
4. 비용 표 실제 금액, 제휴 법무법인 수 입력 (현재 `[00,000]원` 자리표시)
5. 로고·사진 확보 후 히어로/서비스 안내에 반영
6. 서비스 안내를 서류별 상세(필요 서류, 준비물, 국가별 유의사항)로 확장
7. 신청 폼 확장: 부수, 희망 수령일, 수령 방법(방문/등기/해외 발송), 파일 업로드
   (파일은 Supabase Storage 를 쓰면 된다)
8. 사업자 정보·연락처·개인정보 처리방침 등 푸터 확정
9. 신규 신청 알림(이메일/카카오) — 대시보드를 매번 열지 않아도 되게

## 10. 파일

| 경로 | 무엇인가 |
| --- | --- |
| `web/index.html` | 페이지 껍데기 · 상단 내비 · 푸터 |
| `web/app.js` | 화면 4개 · 상태 · 검증 · Supabase 저장 |
| `web/site.css` | 사이트 전용 스타일 (디자인 시스템 위에 얹음) |
| `web/organic.css` | Organic 디자인 시스템 (`_ds/` 에서 복사) |
| `web/config.js` | Supabase URL · anon 키 — **사용자가 채워야 함** |
| `supabase/schema.sql` | 테이블 · 시퀀스 · RLS · 저장 함수 |
| `README.md` | 셋업 · 배포 · 신청서 조회 방법 |
| `.claude/launch.json` | 로컬 미리보기 서버 설정 (포트 5173) |
| `한결 글로벌문서 프로토타입.dc.html` | 초기 디자인 목업 (참고용, 저장 기능 없음) |
| `_ds/organic-.../` | 디자인 시스템 원본 |
| `ios-frame.jsx`, `image-slot.js` | 목업에서 쓰던 부품 |
